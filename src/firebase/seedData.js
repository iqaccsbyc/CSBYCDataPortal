// seedData.js — no longer used for seeding.
// Faculty, departments, and users are managed via the portal UI.
// Kept as an empty module so existing imports don't break during cleanup.

export async function seedDepartments() {}
export async function seedFaculty() {}
export async function seedUsers() {}
export async function seedAll() {}
